# =============================================================
# VOLLEY APP .htaccess
# =============================================================

# Išjungiame failų sąrašo rodymą (Directory Listing)
Options -Indexes

# Nustatome UTF-8 koduotę visiems failams
AddDefaultCharset UTF-8

# Apsaugome jautrius failus nuo tiesioginės prieigos
<FilesMatch "^(database\.sql|SETUP_CHECKLIST\.md|README\.md|\.git)">
    Order allow,deny
    Deny from all
</FilesMatch>

# Priverstinis HTTPS (rekomenduojama, atkomentuokite jei turite SSL)
# RewriteEngine On
# RewriteCond %{HTTPS} off
# RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Slėpti PHP versiją (saugumas)
<IfModule mod_headers.c>
    Header unset X-Powered-By
</IfModule>
